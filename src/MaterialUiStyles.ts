import createTheme from '@mui/material/styles/createTheme';

export const theme = createTheme({
    palette: {
        primary: {
            main: "#3836FD"
        },
    },
})
theme.typography.fontFamily?.link('https://fonts.googleapis.com/css2?family=Dosis&display=swap')
